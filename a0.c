#include <stdio.h>
#include <stdlib.h>

int main () {

    long int L, C, P, T, EDAzinhos;

    scanf(" %ld %ld %ld %ld", &L, &C, &P, &T);
    
    for (int i = 0; i < T; i++) {

    }

    return 0;
}